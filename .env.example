# Splitwise MCP Server Configuration
# Copy this file to .env and fill in your credentials

# ============================================
# Authentication (choose ONE method)
# ============================================

# Option 1: OAuth 2.0 Authentication (Recommended)
# Get these from https://secure.splitwise.com/apps
SPLITWISE_OAUTH_CONSUMER_KEY=your_consumer_key_here
SPLITWISE_OAUTH_CONSUMER_SECRET=your_consumer_secret_here
SPLITWISE_OAUTH_ACCESS_TOKEN=your_access_token_here

# Option 2: API Key Authentication (Alternative)
# SPLITWISE_API_KEY=your_api_key_here

# ============================================
# Optional Configuration
# ============================================

# Cache TTL in seconds (default: 86400 = 24 hours)
# SPLITWISE_CACHE_TTL=86400

# Default fuzzy match threshold for entity resolution (0-100, default: 70)
# SPLITWISE_MATCH_THRESHOLD=70

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# LOG_LEVEL=INFO
